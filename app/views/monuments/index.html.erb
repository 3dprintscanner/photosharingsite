This is the index file

<%= @monuments.each do |monument| %>

	<%= monument.name %>
	<%= monument.description %>

<% end %>

<% if can?(:read, @monuments) %>
	This user can read stuff
<% end %>

<% if user_signed_in? %>
	The user is signed in!
<% end %>